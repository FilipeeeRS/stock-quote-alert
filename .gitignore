# Build artifacts
bin/
obj/
out/

# Real configuration (contém credenciais SMTP — não versionar)
config.json
**/config.json

# Banco de dados (contém e-mails dos inscritos — não versionar)
# Atenção: não use apenas "data/" aqui. Essa regra casa com qualquer pasta
# chamada "data" em qualquer nível e, como o git no Windows não diferencia
# maiúsculas, ela engolia a pasta de código src/StockQuoteAlert/Data/ inteira.
*.db
*.db-shm
*.db-wal

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo

# OS
.DS_Store
Thumbs.db
